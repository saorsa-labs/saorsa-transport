name: WASM portability
on:
  push:
    branches: [main, master, web-support]
  pull_request:
jobs:
  wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - run: cargo check --lib --no-default-features --target wasm32-unknown-unknown
      - run: cargo check -p saorsa-transport --lib --no-default-features --features webrtc --target wasm32-unknown-unknown
